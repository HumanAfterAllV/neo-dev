import ButtonContact from "@/components/elements/ButtonContact";

export default function ContactButtonSection(): React.JSX.Element {
    return (
        <section className="contact-cta relative w-screen h-svh p-[2em] flex justify-center items-center">
            <ButtonContact />
        </section>
    )
}